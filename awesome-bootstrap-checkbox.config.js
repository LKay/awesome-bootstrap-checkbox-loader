module.exports = {
    // Compiler (sass/less)
    compiler : "sass",

    // Default for the style loading
    styleLoader: "style-loader!css-loader!sass-loader",

    styles: {
        checkbox : true,
        radio    : true
    }
}
